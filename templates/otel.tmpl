// Code generated by pkgen; DO NOT EDIT.
package {{ .Name }}

import (
	"sync"

	"go.opentelemetry.io/otel"
	"go.opentelemetry.io/otel/log"
	"go.opentelemetry.io/otel/log/global"
	"go.opentelemetry.io/otel/metric"
	"go.opentelemetry.io/otel/trace"
)

const packagePath = "{{ .PkgPath }}"

var (
	trc      trace.Tracer
	mtr      metric.Meter
	lgr      log.Logger
	otelOnce sync.Once
)

func otelInit() {
	otelOnce.Do(func() {
		trc = otel.Tracer(packagePath)
		mtr = otel.Meter(packagePath)
		lgr = global.Logger(packagePath)
	})
}

func packageTracer() trace.Tracer {
	otelInit()
	return trc
}

func packageMeter() metric.Meter {
	otelInit()
	return mtr
}

func packageLogger() log.Logger {
	otelInit()
	return lgr
}
